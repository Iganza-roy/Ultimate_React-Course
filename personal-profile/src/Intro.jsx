import './App.css';
const Intro = () => {
  return (
    <div>
      <h1>Roy Kibata</h1>
      <p> 
        also, backend team set up the project repos in their accounts. i dont
        want to fork them. but rather create a copy in my local machine and then
        push it to an empty repo in my account. guide me through the same
      </p>
    </div>
  );
};

export default Intro;
